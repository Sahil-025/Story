In PowerShell, ls is an alias for Get-ChildItem, but unlike in Linux, you can't use -a as a shorthand flag. To list all files, including hidden files and directories, you need to use the -Force parameter.